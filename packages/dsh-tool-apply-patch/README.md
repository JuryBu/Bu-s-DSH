# DSH apply_patch 候选插件

该插件只装入 Stardust 主力模式：隐藏并执行层封锁官方 `edit`，注册中文 `apply_patch`，官方标准模式保持原样作为救援入口。

补丁在任何写入前完成语法、路径、文件类型、当前内容和全部补丁块校验。已有文件通过 DSH `ctx.fs` 的版本守卫写入；中途失败时按刚写出的版本逆序恢复。DSH rc.6 尚无安全删除接口，因此插件在预检阶段拒绝 `Delete File`。为避免新增文件无法回滚，一份补丁最多新增一个文件，并把它排在所有更新之后执行。
