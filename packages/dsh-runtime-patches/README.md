# DSH 0.1.0-rc.6 运行时补丁

本包只对候选 release 做可重复、遇到上游差异就停止的文本转换，不直接修改生产目录。当前负责：

- 把运行状态外框、工作区规则基线、更新通知、预算和错误日志转为中文；
- 让包含 `<!-- dsh:ignore -->` 的 `AGENTS.md` 或其它候选规则文件明确退出 DSH 注入；
- 保留官方标准模式和未命中的第三方内容，不能用模糊替换静默改错上游源码。

候选构建必须传入已锁定的 DSH release，并在修改前后记录文件 SHA-256。若任一预期片段不再唯一，构建立即失败。
